{
  "name": "noslothcv",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone card-feed UI for HCV-1000 style scanning on real DS/3DS over Wi-Fi (unofficial).",
  "scripts": {
    "setup": "npm --prefix client install && npm --prefix client run build",
    "build": "npm --prefix client run build",
    "start": "node server/index.js",
    "scan": "bash scripts/scan-secrets.sh",
    "test": "node server/lib/keybaker.test.js",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "node server/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
